#include<stdio.h>
#include<conio.h>

int main()
{
    int i[5], No;

    for(No = 0 ; No < 5 ; No++)
    {
        printf("\n Display Elements Are : %d",i[No]);
    }

    _getch();
    return 0;
}
